; Pool do php-fpm. O nginx conversa com ele por TCP em 127.0.0.1:9000.

[www]
user = nginx
group = nginx

listen = 127.0.0.1:9000

pm = dynamic
pm.max_children = 10
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
; Recicla o processo periodicamente para evitar vazamento de memória
pm.max_requests = 500

; Logs vão para o stdout do container, lidos pelo Coolify
catch_workers_output = yes
decorate_workers_output = no

; Repassa as variáveis de ambiente do container para a aplicação
clear_env = no
